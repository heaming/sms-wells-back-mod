<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kyowon.sms.wells.web.promotion.common.mapper.WpmzPromotionCheckMapper">
    <select id="selectProductClassificationInfo" resultType="String">
       SELECT PD_HCLSF_ID || '>' || PD_MCLSF_ID || '>' || PD_LCLSF_ID || '>' || PD_DCLSF_ID
          FROM TB_PDBS_PD_BAS
      WHERE PD_CD = #{pdCd}
    </select>
</mapper>
